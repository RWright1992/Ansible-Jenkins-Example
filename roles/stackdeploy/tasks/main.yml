---
# tasks file for stackdeploy
- name:
  pip:
    name:
    - jsondiff
    - pyyaml
    
- name: Deploy stack from a compose file
  docker_stack:
    state: present
    name: mystack
    compose:
      - /home/user/trio/docker-compose.yaml